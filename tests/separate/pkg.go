// Package separate defines separate submodule guarded by separate tag. It is separated because of all included
// unit tests are meant to fail. So one might have all explicitly failing unit tests separate from all other
// unit tests.
package separate
